# 펫 스프라이트 이미지

이 폴더에 `{id}.png`를 넣으면 그 종은 절차적 3D 렌더링 대신 이미지로 표시됩니다.
파일이 없는 종은 기존 절차적 렌더러로 자동 대체되므로, 한 장씩 넣어가며 교체할 수 있습니다.

- 파일명: `1.png` ~ `42.png` (펫 id)
- 권장 규격: 256×256 PNG, 배경 투명 또는 흰색
- 각 id에 해당하는 생성 프롬프트는 `prompts.json`에 있습니다

넣는 방법: 이 폴더에 파일을 두고 커밋하면 끝입니다. 코드 수정은 필요 없습니다.
