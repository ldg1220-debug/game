# 프로젝트: STONEAGE-REBUILD

## 목표
2000년대 초 PC 온라인 게임 「스톤에이지」의 코어 게임플레이를 현대 웹 스택으로
재구현한다. 1차 목표는 **싱글플레이 프로토타입**이며, 멀티플레이는 아키텍처상
확장 가능하도록 설계만 열어둔다. IP·에셋·고유명사를 그대로 쓰지 않고, 시스템
구조만 계승한다(펫 이름/외형/맵 지명은 자체 창작).

## 기술 스택 (변경 금지)
- 프론트엔드: React 18 + TypeScript + Vite
- 렌더링: HTML5 Canvas (PixiJS 사용 가능). DOM 기반 타일 렌더링 금지
- 상태관리: Zustand
- 백엔드: FastAPI + SQLModel + SQLite (개발) / PostgreSQL (배포)
- 전투 엔진: 순수 TypeScript. React/DOM/브라우저 API 의존성 0
- 테스트: Vitest (엔진), pytest (백엔드)

## 절대 규칙
1. **전투 엔진은 결정론적이어야 한다.** 모든 난수는 주입된 seeded RNG를 통해서만
   발생한다. `Math.random()` 직접 호출 금지. 같은 seed + 같은 입력 = 같은 결과.
2. **밸런스 수치는 코드에 하드코딩하지 않는다.** 전부 `/data/*.json` 에 둔다.
   코드는 데이터를 읽을 뿐이다.
3. **성장률 상한은 시스템 상수이며, 어떤 아이템/결제/이벤트로도 초과할 수 없다.**
   상한 초과 로직을 작성하려는 시도가 있으면 거부하고 이유를 설명한다.
4. 한 번에 하나의 Phase만 작업한다. 요청받지 않은 파일을 수정하지 않는다.
5. 각 Phase 종료 시 반드시 테스트를 작성하고 실행 결과를 보고한다.
6. 주석과 커밋 메시지는 한국어로 작성한다.

## 디렉터리 구조
```
/src
  /engine        ← 순수 로직 (전투, 성장, 포획, 확률). 프레임워크 의존 0
    /battle
    /growth
    /capture
    rng.ts
  /game          ← 게임 루프, 맵, 인카운터, 세이브
  /render        ← Canvas 렌더러
  /ui            ← React 컴포넌트
  /data          ← 밸런스 JSON (pets, skills, items, maps, spirits)
/server          ← FastAPI
/tests
/tools           ← 밸런스 시뮬레이터 CLI
```

## 용어 사전
- 성장률(growthRate): 레벨업 시 능력치 상승폭을 나타내는 개체 고유값
- 충성도(loyalty): 펫 전용 스탯. 낮으면 불복종/도주
- 기력(spirit): 정령(주술) 사용 자원
- 인카운터(encounter): 필드 이동 중 발생하는 랜덤 전투

## 설계 근거 — 반드시 방어할 지점

원작은 캐시펫이 기존 펫의 성장률 상한을 넘어선 순간 코어 루프가 무너졌다.
구펫 상한이 약 5.1이었는데 캐시펫이 이를 초과하면서, 유저가 수백 시간 투자한
결과물이 결제 몇 번으로 대체 가능해졌다.

그래서 `GROWTH_SCORE_CAP`은 시스템 상수이고, 결제로는 **시도 횟수만** 살 수 있다.
상한 자체는 살 수 없다. 이 규칙을 우회하는 코드는 작성하지 않는다.

## 원작 대비 의도적으로 바꾸는 것

| 원작의 문제 | 대안 |
|---|---|
| 캐시펫 성장률 인플레 | 성장률 상한을 하드 캡으로 코드에 고정 |
| 신규 유저 진입 장벽 | 레벨 구간별 전투력 상한(브래킷) + 신규 구간 별도 필드 |
| 방생/재포획 노가다 | 성장률 재추첨을 소모 아이템으로 부분 허용 |
| 거래 사기 복구 부재 | 에스크로 거래 + 서버 로그 기반 롤백 |
| 순수 랜덤 인카운터의 피로 | 인카운터에 "위험도 게이지" 예고 신호 삽입 |

## Phase 진행 상황

- [x] **Phase 0** 프로젝트 셋업 · seeded RNG · Vitest · FastAPI 스켈레톤
- [x] **Phase 1** 데이터 모델과 밸런스 시트 · zod 검증 게이트
- [x] **Phase 2** 전투 엔진 (결정론 · 진형 · 상태이상 · 포획/도주)
- [x] **Phase 3** 성장 · 포획 · 충성도
- [x] **Phase 4** 맵 · 이동 · 인카운터 (첫 화면)
- [ ] Phase 5 인벤토리 · 장비 · 정령 · 상점
- [ ] Phase 6 퀘스트 · NPC · 진화
- [ ] Phase 7 저장 · 밸런스 시뮬레이터
- [ ] Phase 8 멀티플레이 (선택)

## 데이터 검증 게이트

`src/data/*.json` 은 손으로 고칠 수 있으므로, 잘못된 수치가 배포까지 흘러가지
않도록 빌드가 먼저 검증을 통과해야 한다.

```
pnpm gen:data       # 밸런스 시트에서 JSON 재생성
pnpm validate:data  # 스키마 + 불변식 + 참조 무결성 검사
pnpm build          # validate:data → tsc → vite build 순서. 위반 시 여기서 멈춘다
```

검사 층위는 `src/data/schema.ts` 에 있다.
1. 스키마 — 필드/타입/범위 (zod, 모르는 필드도 거부)
2. 불변식 — 성장률 상한, min≤max, 아키타입별 필수 필드, 정령 레벨 단조성
3. 참조 무결성 — skillPool · evolveTo · spiritId

`tests/data.test.ts` 는 통과 사례만이 아니라 **위반을 실제로 잡아내는지**도
검사한다(상한 초과, 미지 필드, 없는 참조, 대가 없는 공격 버프 등).

## 전투 엔진

```ts
simulateBattle({ allies, enemies, seed }) → { log, winner, finalState, capturedPet? }
```

`src/engine/battle/` — 순수 TypeScript다. import 하는 건 자기 자신과 `src/data/*.json`
뿐이고, React·DOM·브라우저 API는 하나도 없다. 입력 객체를 mutate 하지 않는다.

- `types.ts` 구조와 `BattleEvent` 목록
- `formula.ts` 수식의 **모양**만. 숫자는 `src/data/formula.json` 에 있다
- `catalog.ts` 데이터를 엔진이 쓰는 모양으로. 주입식이라 테스트가 가짜를 넣을 수 있다
- `ai.ts` 기본 커맨드 소스. UI가 붙으면 아군 쪽만 플레이어 입력으로 갈아낀다
- `index.ts` `simulateBattle`

로그로 전투를 재생할 수 있는지는 콘솔 뷰어로 확인한다. 이 뷰어는 상태를 다시
계산하지 않고 `BattleEvent` 만 읽는다 — 렌더러가 지켜야 할 선의 실물 예시다.

```
pnpm battle:demo [seed]
```

## 성장 · 포획 · 충성도

`src/engine/growth` · `src/engine/capture` · `src/engine/loyalty.ts`. 상수는
`src/data/growth.json` 에 있고 전투 상수(`formula.json`)와 분리했다 — 전투 수식은
거의 안 건드리지만 성장 곡선과 포획률은 시뮬레이터를 돌릴 때마다 움직인다.

- **성장률 추첨** 종의 범위에서 균등분포. 희귀도 보정도, 아이템 보정도 없다.
  희귀도가 높은 종은 `growthRange` 자체가 높을 뿐이다. 상한을 넘는 범위가
  들어오면 비율로 눌러 담는다(손으로 고친 데이터에 대한 마지막 방어선).
- **레벨업** 소수점을 그대로 누적한다. 매 레벨 반올림하면 성장률 4.9와 5.0의
  차이가 수십 레벨 뒤에 사라져 개체차라는 축이 무너진다. 화면·전투에는
  `battleStats()` 로 내려서 넘긴다.
- **포획** HP·레벨차·도구·매력의 함수. HP에 대해 단조 증가, 레벨차에 대해 단조
  감소인 것을 테스트로 고정했다. 상한 0.95는 규칙이다 — 확실한 포획이 있으면
  "더 깎을까 지금 잡을까"의 긴장이 사라진다.
- **충성도** 70 미만 불복종 / 30 미만 적대 / 10 미만 도주. 하락이 회복보다
  빠르다(검증기가 강제한다). **도주는 되돌릴 수 없고, 되돌리는 경로를 만들지 않는다.**

전투 엔진과 포획 모듈은 서로를 모른다. 잇는 어댑터는 `createCaptureResolver()`
하나뿐이라 어느 쪽을 바꿔도 다른 쪽이 흔들리지 않는다.

## 필드 · 화면

첫 화면이 나오는 단계다. `pnpm dev` 로 띄운다.

```
src/game/     맵·이동·인카운터·태세·재생 — 전부 순수 함수 + Zustand 스토어 하나
src/render/   Canvas 렌더러. 게임 상태를 읽기만 한다
src/ui/       React 컴포넌트
src/data/maps.json   pnpm gen:maps 로 굽는다
```

**맵** 마을 1 · 필드 3 · 던전 1. 레이아웃은 `tools/gen-maps.mjs` 에 ASCII로
그리고, collision·encounter 레이어를 생성기가 같이 구워낸다 — 세 레이어가 손으로
어긋날 수 없다. 워프는 양방향이어야 하고, 워프 칸에는 존을 칠하지 않는다
(넘나드는 칸은 물러설 발판이어야 한다).

**이동** 8방향 타일 스냅. 한 번 움직이면 반드시 다음 칸 중앙까지 간다 — 그래야
걸음 수를 셀 수 있고, 걸음 수가 세져야 위험도 게이지를 정직하게 보여줄 수 있다.
대각선은 양옆이 모두 뚫려야 통과한다.

**인카운터** 순수 랜덤 대신 위험도 게이지. 걸음마다 존의 비율만큼 차오르고
화면에 그대로 노출된다. 언제 걸릴지 대략 알 수 있으니 "지금 돌아갈까, 한 판 더
할까"가 판단이 된다. 무작위성은 남기되(걸음당 상승분이 흔들린다) 예측 불가능만
없앤다. 전투 직후에는 유예 걸음이 있고, 안전 지대를 걸으면 가라앉는다.

**전투 진입** 전투 엔진은 한 번 부르면 끝까지 굴러간다(결정론과 서버 검증을 위해
그렇게 설계했고, 헌장 Phase 8도 클라이언트가 seed와 로그만 받아 재생한다고
못박는다). 그래서 플레이어 선택은 라운드별 커맨드가 아니라 **태세**로 들어간다 —
공격 / 포획 / 수비 / 도주. 포획 태세는 "깎다가 충분히 약해지면 밧줄을 던진다"로
구현해, 원작의 "지금 잡을까 더 깎을까" 긴장을 한 전투 안에 남긴다.

**재생** `src/game/replay.ts` 가 BattleEvent 배열만 접어 임의 시점 상태를 만든다.
엔진을 다시 돌리지 않는다. 로그가 충분한지는 테스트가 지킨다 — 끝까지 재생한
결과가 엔진의 finalState와 정확히 같아야 한다. 어긋나면 렌더러가 아니라 엔진에서
고칠 문제다.

전투 결과(경험치·충성도·포획)는 재생이 끝나야 반영된다. 바로 반영하면 로그가
아직 1라운드를 보여주는 동안 파티 목록에 잡은 펫이 떠서 결과를 미리 흘린다.

## 기존 `client/` 디렉터리에 관해

이 저장소에는 헌장 이전에 만든 프로토타입이 `client/` 에 있다. 스택도 구조도
헌장과 다르다(상태관리 Context, 6스탯, Math.random 직접 사용, 밸런스 하드코딩,
테스트 없음). 헌장의 구조로 다시 세우는 중이며, 두 트리는 당분간 공존한다.

`client/public/pets/` 의 스프라이트 이미지와 `prompts.json` 은 새 트리에서도
그대로 쓸 수 있는 자산이므로 보존한다.
