# 프로젝트: STONEAGE-REBUILD

## 목표
2000년대 초 PC 온라인 게임 「스톤에이지」의 코어 게임플레이를 현대 웹 스택으로
재구현한다. 1차 목표는 **싱글플레이 프로토타입**이며, 멀티플레이는 아키텍처상
확장 가능하도록 설계만 열어둔다. IP·에셋·고유명사를 그대로 쓰지 않고, 시스템
구조만 계승한다(펫 이름/외형/맵 지명은 자체 창작).

## 기술 스택 (변경 금지)
- 프론트엔드: React 18 + TypeScript + Vite
- 렌더링: HTML5 Canvas (PixiJS 사용 가능). DOM 기반 타일 렌더링 금지
- 상태관리: Zustand
- 백엔드: FastAPI + SQLModel + SQLite (개발) / PostgreSQL (배포)
- 전투 엔진: 순수 TypeScript. React/DOM/브라우저 API 의존성 0
- 테스트: Vitest (엔진), pytest (백엔드)

## 절대 규칙
1. **전투 엔진은 결정론적이어야 한다.** 모든 난수는 주입된 seeded RNG를 통해서만
   발생한다. `Math.random()` 직접 호출 금지. 같은 seed + 같은 입력 = 같은 결과.
2. **밸런스 수치는 코드에 하드코딩하지 않는다.** 전부 `/data/*.json` 에 둔다.
   코드는 데이터를 읽을 뿐이다.
3. **성장률 상한은 시스템 상수이며, 어떤 아이템/결제/이벤트로도 초과할 수 없다.**
   상한 초과 로직을 작성하려는 시도가 있으면 거부하고 이유를 설명한다.
4. 한 번에 하나의 Phase만 작업한다. 요청받지 않은 파일을 수정하지 않는다.
5. 각 Phase 종료 시 반드시 테스트를 작성하고 실행 결과를 보고한다.
6. 주석과 커밋 메시지는 한국어로 작성한다.

## 디렉터리 구조
```
/src
  /engine        ← 순수 로직 (전투, 성장, 포획, 확률). 프레임워크 의존 0
    /battle
    /growth
    /capture
    rng.ts
  /game          ← 게임 루프, 맵, 인카운터, 세이브
  /render        ← Canvas 렌더러
  /ui            ← React 컴포넌트
  /data          ← 밸런스 JSON (pets, skills, items, maps, spirits)
/server          ← FastAPI (상태: 세이브·대전 기록·거래·길드·연결)
/arbiter         ← 판정 서비스 (규칙: 검증·대전 판정). 클라이언트와 같은 엔진을 import 한다
/tests
/tools           ← 밸런스 시뮬레이터 CLI
```

## 용어 사전
- 성장률(growthRate): 레벨업 시 능력치 상승폭을 나타내는 개체 고유값
- 충성도(loyalty): 펫 전용 스탯. 낮으면 불복종/도주
- 기력(spirit): 정령(주술) 사용 자원
- 인카운터(encounter): 필드 이동 중 발생하는 랜덤 전투

## 설계 근거 — 반드시 방어할 지점

원작은 캐시펫이 기존 펫의 성장률 상한을 넘어선 순간 코어 루프가 무너졌다.
구펫 상한이 약 5.1이었는데 캐시펫이 이를 초과하면서, 유저가 수백 시간 투자한
결과물이 결제 몇 번으로 대체 가능해졌다.

그래서 `GROWTH_SCORE_CAP`은 시스템 상수이고, 결제로는 **시도 횟수만** 살 수 있다.
상한 자체는 살 수 없다. 이 규칙을 우회하는 코드는 작성하지 않는다.

## 원작 대비 의도적으로 바꾸는 것

| 원작의 문제 | 대안 |
|---|---|
| 캐시펫 성장률 인플레 | 성장률 상한을 하드 캡으로 코드에 고정 |
| 신규 유저 진입 장벽 | 레벨 구간별 전투력 상한(브래킷) + 신규 구간 별도 필드 |
| 방생/재포획 노가다 | 성장률 재추첨을 소모 아이템으로 부분 허용 |
| 거래 사기 복구 부재 | 에스크로 거래 + 서버 로그 기반 롤백 |
| 순수 랜덤 인카운터의 피로 | 인카운터에 "위험도 게이지" 예고 신호 삽입 |

## Phase 진행 상황

- [x] **Phase 0** 프로젝트 셋업 · seeded RNG · Vitest · FastAPI 스켈레톤
- [x] **Phase 1** 데이터 모델과 밸런스 시트 · zod 검증 게이트
- [x] **Phase 2** 전투 엔진 (결정론 · 진형 · 상태이상 · 포획/도주)
- [x] **Phase 3** 성장 · 포획 · 충성도
- [x] **Phase 4** 맵 · 이동 · 인카운터 (첫 화면)
- [x] **Phase 5** 인벤토리 · 장비 · 정령 · 상점
- [x] **Phase 6** 퀘스트 · NPC · 진화 · 도감
- [x] **Phase 7** 저장 · 밸런스 시뮬레이터
- [x] **Phase 8** 멀티플레이 — 서버 권위 대전 · 에스크로 거래 · 길드 · 실시간 동기화
- [x] **Phase 8 보강** 계정·토큰 인증 — 행위자를 본문이 아니라 토큰이 정하게 함

## 데이터 검증 게이트

`src/data/*.json` 은 손으로 고칠 수 있으므로, 잘못된 수치가 배포까지 흘러가지
않도록 빌드가 먼저 검증을 통과해야 한다.

```
pnpm gen:data       # 밸런스 시트에서 JSON 재생성
pnpm validate:data  # 스키마 + 불변식 + 참조 무결성 검사
pnpm build          # validate:data → tsc → vite build 순서. 위반 시 여기서 멈춘다
```

검사 층위는 `src/data/schema.ts` 에 있다.
1. 스키마 — 필드/타입/범위 (zod, 모르는 필드도 거부)
2. 불변식 — 성장률 상한, min≤max, 아키타입별 필수 필드, 정령 레벨 단조성
3. 참조 무결성 — skillPool · evolveTo · spiritId

`tests/data.test.ts` 는 통과 사례만이 아니라 **위반을 실제로 잡아내는지**도
검사한다(상한 초과, 미지 필드, 없는 참조, 대가 없는 공격 버프 등).

## 전투 엔진

```ts
simulateBattle({ allies, enemies, seed }) → { log, winner, finalState, capturedPet? }
```

`src/engine/battle/` — 순수 TypeScript다. import 하는 건 자기 자신과 `src/data/*.json`
뿐이고, React·DOM·브라우저 API는 하나도 없다. 입력 객체를 mutate 하지 않는다.

- `types.ts` 구조와 `BattleEvent` 목록
- `formula.ts` 수식의 **모양**만. 숫자는 `src/data/formula.json` 에 있다
- `catalog.ts` 데이터를 엔진이 쓰는 모양으로. 주입식이라 테스트가 가짜를 넣을 수 있다
- `ai.ts` 기본 커맨드 소스. UI가 붙으면 아군 쪽만 플레이어 입력으로 갈아낀다
- `index.ts` `simulateBattle`

로그로 전투를 재생할 수 있는지는 콘솔 뷰어로 확인한다. 이 뷰어는 상태를 다시
계산하지 않고 `BattleEvent` 만 읽는다 — 렌더러가 지켜야 할 선의 실물 예시다.

```
pnpm battle:demo [seed]
```

## 성장 · 포획 · 충성도

`src/engine/growth` · `src/engine/capture` · `src/engine/loyalty.ts`. 상수는
`src/data/growth.json` 에 있고 전투 상수(`formula.json`)와 분리했다 — 전투 수식은
거의 안 건드리지만 성장 곡선과 포획률은 시뮬레이터를 돌릴 때마다 움직인다.

- **성장률 추첨** 종의 범위에서 균등분포. 희귀도 보정도, 아이템 보정도 없다.
  희귀도가 높은 종은 `growthRange` 자체가 높을 뿐이다. 상한을 넘는 범위가
  들어오면 비율로 눌러 담는다(손으로 고친 데이터에 대한 마지막 방어선).
- **레벨업** 소수점을 그대로 누적한다. 매 레벨 반올림하면 성장률 4.9와 5.0의
  차이가 수십 레벨 뒤에 사라져 개체차라는 축이 무너진다. 화면·전투에는
  `battleStats()` 로 내려서 넘긴다.
- **포획** HP·레벨차·도구·매력의 함수. HP에 대해 단조 증가, 레벨차에 대해 단조
  감소인 것을 테스트로 고정했다. 상한 0.95는 규칙이다 — 확실한 포획이 있으면
  "더 깎을까 지금 잡을까"의 긴장이 사라진다.
- **충성도** 70 미만 불복종 / 30 미만 적대 / 10 미만 도주. 하락이 회복보다
  빠르다(검증기가 강제한다). **도주는 되돌릴 수 없고, 되돌리는 경로를 만들지 않는다.**

전투 엔진과 포획 모듈은 서로를 모른다. 잇는 어댑터는 `createCaptureResolver()`
하나뿐이라 어느 쪽을 바꿔도 다른 쪽이 흔들리지 않는다.

## 필드 · 화면

첫 화면이 나오는 단계다. `pnpm dev` 로 띄운다.

```
src/game/     맵·이동·인카운터·태세·재생 — 전부 순수 함수 + Zustand 스토어 하나
src/render/   Canvas 렌더러. 게임 상태를 읽기만 한다
src/ui/       React 컴포넌트
src/data/maps.json   pnpm gen:maps 로 굽는다
```

**맵** 마을 1 · 필드 3 · 던전 1. 레이아웃은 `tools/gen-maps.mjs` 에 ASCII로
그리고, collision·encounter 레이어를 생성기가 같이 구워낸다 — 세 레이어가 손으로
어긋날 수 없다. 워프는 양방향이어야 하고, 워프 칸에는 존을 칠하지 않는다
(넘나드는 칸은 물러설 발판이어야 한다).

**이동** 8방향 타일 스냅. 한 번 움직이면 반드시 다음 칸 중앙까지 간다 — 그래야
걸음 수를 셀 수 있고, 걸음 수가 세져야 위험도 게이지를 정직하게 보여줄 수 있다.
대각선은 양옆이 모두 뚫려야 통과한다.

**인카운터** 순수 랜덤 대신 위험도 게이지. 걸음마다 존의 비율만큼 차오르고
화면에 그대로 노출된다. 언제 걸릴지 대략 알 수 있으니 "지금 돌아갈까, 한 판 더
할까"가 판단이 된다. 무작위성은 남기되(걸음당 상승분이 흔들린다) 예측 불가능만
없앤다. 전투 직후에는 유예 걸음이 있고, 안전 지대를 걸으면 가라앉는다.

**전투 진입** 전투 엔진은 한 번 부르면 끝까지 굴러간다(결정론과 서버 검증을 위해
그렇게 설계했고, 헌장 Phase 8도 클라이언트가 seed와 로그만 받아 재생한다고
못박는다). 그래서 플레이어 선택은 라운드별 커맨드가 아니라 **태세**로 들어간다 —
공격 / 포획 / 수비 / 도주. 포획 태세는 "깎다가 충분히 약해지면 밧줄을 던진다"로
구현해, 원작의 "지금 잡을까 더 깎을까" 긴장을 한 전투 안에 남긴다.

**재생** `src/game/replay.ts` 가 BattleEvent 배열만 접어 임의 시점 상태를 만든다.
엔진을 다시 돌리지 않는다. 로그가 충분한지는 테스트가 지킨다 — 끝까지 재생한
결과가 엔진의 finalState와 정확히 같아야 한다. 어긋나면 렌더러가 아니라 엔진에서
고칠 문제다.

전투 결과(경험치·충성도·포획)는 재생이 끝나야 반영된다. 바로 반영하면 로그가
아직 1라운드를 보여주는 동안 파티 목록에 잡은 펫이 떠서 결과를 미리 흘린다.

## 소지품 · 장비 · 정령 · 상점

`src/game/inventory.ts` · `equipment.ts` · `shop.ts`. 상수는 `src/data/economy.json`.
필드에서 <kbd>I</kbd>로 소지품, 마을 문 앞에 서면 상점이 열린다.

**가방** 무게와 슬롯 둘 다 제한한다. 제한이 없으면 다 사서 다 들고 다니면 되고,
그러면 상점도 회복 아이템도 의미가 없다. 부분적으로만 들어가는 경우를 정확히
세어 돌려준다 — "3개 중 1개만 들어갔다"가 조용히 "아무것도 안 들어갔다"가 되면
유저는 아이템을 잃었다고 느낀다.

**장비** 무기·방어구·장신구 3슬롯. **펫은 낄 수 없다**(원작 규칙). 낀 물건은
가방에서 빠지고 교체하면 원래 것이 돌아온다. 장비 무게도 몸에 얹혀 있다 —
끼면 가벼워지는 가방은 말이 안 된다.

**정령** 캐릭터가 아니라 **아이템에 깃든다.** 낀 장비의 정령만 전투에서 쓸 수
있고, 벗으면 같이 사라진다. 전투 중에는 장비를 못 바꾸므로 "무엇을 끼고
나가느냐"가 곧 전투 준비가 된다. 태세가 이걸 실제로 쓴다 — 공격 태세는 피해·
낙인 정령을, 수비 태세는 회복·버프 정령을 기력이 닿는 가장 높은 레벨로 쓴다.

**상점** 파는 값 < 사는 값(검증기가 강제한다). 같으면 왕복 거래만으로 무한히
돈이 늘고 필드에 나갈 이유가 사라진다. 끼고 있는 장비는 가방에 없으므로 목록에
뜨지 않는다 — 입고 있는 갑옷을 실수로 파는 사고가 구조적으로 불가능하다.

## 퀘스트 · NPC · 진화 · 도감

`src/game/quests.ts` · `dialogue.ts` · `dex.ts` · `src/engine/growth/evolve.ts`.
데이터는 `src/data/quests.json` · `dialogue.json`.

**퀘스트** 조건(레벨/아이템/처치/포획/방문) → 보상. 진행도를 **사건**으로 올린다 —
전투가 끝날 때 사건 하나를 흘려보내면 해당하는 목표만 오른다. 세는 곳이
한 곳뿐이라 어긋날 자리가 없다. 누적으로 세는 건 처치·포획뿐이고, 소지·레벨·
방문은 매번 지금 세계를 다시 본다(아이템을 쓰면 목표도 되돌아간다).

**대화** JSON 노드를 걷는다. 조건을 만족하지 않는 선택지는 아예 안 보인다 —
못 고르는 걸 회색으로 늘어놓으면 무엇을 해야 하는지가 오히려 흐려진다.
대화 엔진은 행동을 **실행하지 않고 알려주기만** 한다. 실제로 아이템을 주는 건
스토어가 하므로, 대화를 미리 훑어보거나 되감아도 부작용이 없다.

NPC는 맵에 그려지고 퀘스트가 있으면 머리 위에 표식이 뜬다(`!` 받을 수 있음 /
`?` 보고 가능). 표식이 없으면 누가 어디 있는지 알 방법이 밟아보는 것뿐이다.

**진화** 헌장이 나눈 두 종류를 구분해 구현했다.

| | 이전 성장률 | 조건 |
|---|---|---|
| 퀘스트 진화(진화의 돌) | **무관** — 완전 재추첨 | 레벨 + 주술사 퀘스트 + 진화의 돌 |
| 촉진 진화(촉진제) | **가중치 0.5로 반영** | 레벨 + 촉진제 |

반영 방식이 핵심이다. 이전 성장률 **값**을 섞으면 안 된다 — 진화체는 성장 범위
자체가 높으므로, 낮은 범위에서 나온 값을 섞는 순간 좋은 개체일수록 손해를 본다.
그래서 섞는 건 값이 아니라 **범위 안에서의 상대 위치**다. 제 종에서 상위였던
개체는 진화 후에도 상위에서 시작한다. 결과는 언제나 진화체의 범위 안에 있으므로
**성장률 상한을 구조적으로 넘을 수 없다. 진화는 상한을 여는 문이 아니다.**

재추첨의 물약은 같은 종 안에서 성장률만 다시 뽑는다. 도박성은 남기고 방생·
재포획의 시간 낭비만 없앤다. 상한은 오르지 않는다.

**도감** 본 것과 잡은 것을 따로 센다. 못 잡은 종도 자리를 차지한다 — 빈칸이
보여야 다시 나갈 이유가 생긴다.

조작: <kbd>I</kbd> 소지품(가방/펫/의뢰) · <kbd>P</kbd> 도감 · <kbd>Esc</kbd> 닫기

## 저장 · 밸런스 시뮬레이터

### 세이브

`src/game/save.ts` — 버전 필드와 마이그레이션이 있다. 지금 버전은 2이고,
1→2 마이그레이션이 실제로 산다(Phase 5에서 `character.stats`가 `baseStats`로
갈라지고 소지품·경제·퀘스트·도감이 들어왔다).

두 가지 원칙:
- **일시적인 것은 담지 않는다.** 열려 있던 상점·대화 노드·진행 중인 전투는
  저장하지 않는다. 담으면 로드 직후 참조가 끊긴 화면에서 시작한다.
- **깨진 세이브가 게임을 깨뜨리지 않는다.** 로드는 던지지 않고 이유를 돌려준다.
  남의 파일을 올리거나 손으로 고쳐 망가뜨리는 건 흔한 일이다.

브라우저 저장소가 아니라 **파일**이다. localStorage는 캐시를 지우면 같이 날아가고
유저는 그게 세이브인 줄 모른다. 서버 보관은 `/saves` API에 따로 있다.

`server/` — FastAPI + SQLModel. 세이브를 **해석하지 않고** 통짜 JSON으로 보관한다.
스키마가 바뀔 때마다 서버를 같이 고쳐야 하면 두 곳이 어긋나는 건 시간문제고,
어긋난 쪽이 세이브를 깨뜨린다. 버전과 마이그레이션은 클라이언트 한 곳에만 둔다.
덮어쓰지 않고 5세대까지 쌓는다 — 잘못 저장했을 때 되돌릴 수 있어야 한다.

### 밸런스 시뮬레이터

```
pnpm simulate            # 전부
pnpm simulate growth     # 성장률 추첨 분포
pnpm simulate curve      # 레벨 1~99 전투력 커브 → CSV
pnpm simulate matrix     # 전 종 1대1 승률 + 속성 상성 실측
pnpm simulate party      # 3대3 — 전체공격·도발·회복이 값을 하는 규모
pnpm simulate match A B  # 두 종 맞대결 상세
```

**이 도구로 실제로 튜닝했다.** 첫 실행에서 나온 것:

- 같은 등급 안에서 60%를 넘는 조합 **58건**, 승률 폭 8.2%~82.6%
- 원인 셋을 찾아 고쳤다:
  1. **AI 결함** — 전체공격에 고정 가산점(×1.6)을 줘서 1대1에서도 돌풍(0.82)을
     몸통치기(1.0)보다 먼저 골랐다. 살아 있는 적 수에 비례하도록 고쳤다.
  2. **데이터 공백** — 풍 속성에 단일공격 스킬이 **아예 없었다.** 풍 종은
     전체공격밖에 못 썼다. `칼바람`을 추가했다.
  3. **예산 불균형** — 스탯 총량이 같은 등급 안에서 최대 0.54 차이났다.
     가중치를 **측정**해서(공 1.35 / 체 1.00 / 순 0.97 / 방 0.70) 등급별 총량을
     맞췄다. 짐작으로는 방어가 제일 값나갈 거라 봤는데 실제로는 제일 쌌다.
- 결과: 승률 폭 20.7%~34.1%(common) 로 좁혀졌고, 경고는 **58건 → 5건**.
- 속성 상성이 의도대로 도는 것도 실측으로 확인했다(지 81% vs 수, 수 71% vs 화,
  화 72% vs 풍, 풍 64% vs 지).

**남은 5건은 1대1 규모의 성질이지 밸런스 결함이 아니다.** 3대3으로 다시 재보니
용암달팽이가 37.5% → 50.0%로 오르고 화염멧돼지가 54.8% → 43.1%로 내려가
관계가 뒤집혔다. 1대1에서는 전체공격이 위력 낮은 스킬일 뿐이고, 도발은 지킬
아군이 없어 빈 턴이며, 혼란은 고를 대상이 하나뿐이라 아무 일도 하지 않는다.
실제 게임은 1~5인 파티끼리 붙으므로 1대1 매트릭스만 보고 더 깎으면 파티 규모의
밸런스가 망가진다. 회오리사슴의 회복 우위만 두 규모 모두에서 남아 있다.

## 아이소메트릭 개편

원작 화면과 나란히 놓고 보니 격차가 컸다. 탑다운 32px 정사각 격자에 도형 몇 개로
그린 캐릭터는 원작의 2:1 마름모 격자와 두툼한 스프라이트를 흉내내지 못한다.
투영부터 갈았다.

```
src/render/iso.ts       투영 · 깊이 정렬 · 카메라 · 입력 회전
src/render/palette.ts   속성/종족 색, 사람 색
src/render/sprites.ts   사람과 8골격 생물을 도형으로 조립
src/render/tileset.ts   마름모 바닥 · 경계 블렌딩 · 솟은 오브젝트
```

**게임 로직은 투영을 모른다.** 이동·충돌·인카운터·세이브는 전부 월드 타일 좌표
그대로다. 바뀐 것은 그리기 직전의 변환 한 겹뿐이고, 그래서 밸런스도 리플레이도
세이브 호환성도 손대지 않았다.

**타일** 96×48. 처음 64×32로 잡았더니 마을 하나가 통째로 화면에 들어와 걸어다닐
이유가 사라졌다. 그림은 64×32 기준으로 그리고 `ART_SCALE` 로 한 번 늘린다 —
치수를 전부 타일 크기로 나눠 쓰면 코드가 읽히지 않는다.

**깊이** 정렬 키가 `x + y` 다. 탑다운 시절의 "y로 정렬"을 그대로 옮기면 나무 뒤로
대각선으로 걸어갈 때 안 가려진다.

**입력** 화살표를 월드 축에 그대로 꽂으면 위쪽 키가 화면 오른쪽 위로 간다. 화면
축을 45° 돌려 넣는다. 마침 화살표 넷이 화면 상하좌우가 되고 두 개를 같이 누르면
월드 축 넷이 나와, 여덟 방향이 빠짐없이 겹치지 않게 대응된다.

**외곽선** 도형만 쌓으면 초록 펫이 풀밭에, 회색 골렘이 석판에 묻힌다. 도형마다
stroke를 거는 방법은 도형이 수십 개라 반드시 몇 개를 빠뜨린다. 완성된 실루엣을
어둡게 물들여 여덟 방향으로 한 픽셀씩 깔고 그 위에 원본을 얹는다 — 도형을 추가해도
자동으로 테두리가 생긴다.

**골격** 종마다 그림을 그리는 대신 여덟 골격(beast/horned/shell/saurian/serpent/
ray/bird/golem)으로 나누고 `pets.json` 의 `form` 필드가 고른다. 렌더러 안에
id→외형 표를 숨기면 종을 추가할 때 검증이 못 잡고 조용히 기본 모양으로 나온다.
색과 체형은 속성 + 종 id 해시로 흔들어, 같은 골격이라도 늑대는 길쭉하고 두더지는
뭉툭하다.

**화면으로 확인한 것들** 테스트가 못 잡는 결함이 여럿 있었다.
- 마을 전체가 한 화면에 들어와 탐험할 이유가 없었다 → 타일 96×48
- 집이 지붕 없는 돌 평상으로 보였다 → 초가 윗면 + 높이 54
- 지붕 결을 부챗살로 그렸더니 칸마다 반복돼 삼각 천막이 늘어섰다 → 방향 없는 잔결
- 경계 블렌딩이 0.85로 진해 마을 광장이 통째로 탁했다 → 0.5로 얇게
- 흙길 위의 갈색 옷 주인공이 배경에 묻혔다 → 플레이어 색을 지형에 안 쓰는 붉은색으로 고정
- 나무가 전부 같아 숲 가장자리가 울타리로 보였다 → 오브젝트 변종 3장
- 사람 8방향이 전부 똑같아 보였다 → 앞/옆/뒤로 머리를 통째로 다르게 그리고 몸통 폭을 크게 바꿈
- 문이 지붕 위로 삐져나왔다 → 화면 좌표가 아니라 옆면 위의 좌표로 찍음
- 전투에서 두 진영이 서로 등을 돌리고 있었다 → 진영을 대각선으로 어긋나게 배치
- 후열 이름표를 전열 개체가 덮어 정작 봐야 할 체력이 안 보였다 → 후열 이름표는 머리 위로

24종 스프라이트와 사람 8방향, 전투 장면 두 개를 한 판에 그려 보는 임시 시트로
확인했다. 실제 게임을 조작해 특정 상황을 재현하는 것보다 훨씬 빠르고 빠짐이 없다.

**남은 것** 같은 골격을 쓰는 종은 색과 비례만 다르다. 24종을 다 다르게 만들려면
골격을 더 쪼개거나 스프라이트 이미지를 써야 하는데, 지금 구조에서는 골격 표에 한
줄 붙이는 것으로 확장된다는 이점을 잃는다.

## 스프라이트 — 종이 구별되어야 한다

`src/render/sprites.ts` · `species.ts`. 외부 이미지 없이 캔버스 도형으로 조립한다.
24종에 8방향에 동작까지 그리면 수백 장이고, 그 수백 장이 어긋나기 시작하면 손댈
수가 없다. 도형으로 짜면 종이 늘어도 표에 한 줄 붙는 것으로 끝난다.

**골격만으로는 부족했다.** 처음엔 골격 여덟 가지 + 색(속성 + 종 id 해시)으로
갈랐는데, 스프라이트 시트를 뽑아 늘어놓으니 24종 중 7종이 색만 다른 여우였다.
종을 모으는 게임에서 종이 구별되지 않으면 모을 이유가 없다.

그래서 골격 아래에 **직교하는 특징**을 뒀다 — 귀·꼬리·주둥이·체격, 그리고 갈기·
뿔·엄니·발톱 같은 상징 부속. 조합이 곧 실루엣이다.

| 종 | 무엇으로 구별되는가 |
|---|---|
| 흙두더지 | 삽 주둥이 · 앞발 발톱 · 꼬리 없음 · 땅에 붙은 키 |
| 불씨여우 | 큰 뾰족귀 · 풍성한 꼬리 · 날씬한 몸 |
| 질풍늑대 | 등갈기 · 뾰족귀 · 여우보다 높은 키 |
| 화염멧돼지 | 엄니 · 갈기 · 거대한 몸 · 꼬리 없음 |
| 여울수달 | 납작한 꼬리 · 둥근 귀 |
| 초원토끼 | 긴 귀 · 동그란 뭉치 꼬리 |
| 용암달팽이 | 나선 껍질(동심원이 아니다) |
| 넝쿨거북 | 껍질 테두리 가시 · 꼬리 |
| 심연의군주 | 이어진 등돛(가시가 아니다) · 긴 목 |
| 폭풍매 | 굽은 부리 · 펼친 날개 · 볏 없음 |

`horned` 골격은 없앴다. 뿔이 특징 하나가 되면서 골격으로 둘 이유가 사라졌고,
골격에 두면 "뿔 달린 새"를 표현할 수 없다.

특징표는 `pets.json`이 아니라 렌더 층에 있다 — 순수하게 그리는 방법이고 게임
어디에도 드러나지 않아서, 밸런스 데이터 파일에 "귀 모양" 필드를 24줄 넣으면 그
파일이 무엇을 위한 것인지 흐려진다. 대신 `tests/data.test.ts` 가 넷을 지킨다:
모든 종에 특징이 있는가, 없는 종이 표에 있는가, 특징의 골격이 데이터와 같은가,
그리고 **같은 골격끼리 특징 조합이 겹치지 않는가.** 마지막 것이 핵심이다 —
겹치면 색만 다른 같은 그림이고, 그건 종이 없는 것과 같다.

## 멀티플레이

```
pnpm serve:arbiter                          # 판정 서비스 (Node, 기본 8787)
uvicorn server.main:app --port 8000         # FastAPI
```

### 왜 서버가 둘인가

전투 규칙은 이미 순수 TypeScript로 한 벌 있다. 서버에서 쓰겠다고 파이썬으로 옮겨
적으면 **같은 규칙의 구현이 둘이 된다.** 둘은 반드시 어긋나고, 어긋난 날 어느 쪽이
맞는지 판정할 방법이 없다. 밸런스 수치 하나 고칠 때마다 두 곳을 고쳐야 하고, 한
곳을 빠뜨리면 "서버에서는 이겼는데 화면에서는 졌다"가 된다.

그래서 역할을 나눴다.

| | 맡는 것 |
|---|---|
| FastAPI (`/server`) | 상태 — 세이브·대전 기록·거래·길드·연결 |
| 판정 서비스 (`/arbiter`) | 규칙 — 세이브가 정당한가, 이 seed로 누가 이기는가 |

판정 서비스는 `src/engine/battle` 과 `src/game/verify` 를 **그대로 import** 한다.
복사본이 아니다. 상태를 갖지 않으므로 몇 개를 띄우든 같은 입력에 같은 답이 나온다.
FastAPI는 판정 호출을 의존성으로 주입받아, 판정 서비스가 죽어도 대전만 멈추고
세이브·거래·길드는 계속 돈다(테스트로 고정했다).

### 클라이언트를 믿지 않는 세 가지

**1. seed를 서버가 뽑는다.** 클라이언트가 고르게 하면 유리한 seed가 나올 때까지
미리 돌려보고 그때 요청을 보낼 수 있다. 결정론 엔진에서 그건 곧 결과 조작이다.

**2. 세이브를 요청에 담지 않는다.** 플레이어 id와 태세만 받고 세이브는 서버가
꺼낸다. 요청에 담게 하면 "지금 그 사람의 상태"가 아니라 "그가 주장하는 상태"가 된다.

**3. 능력치를 다시 계산한다.** 처음엔 `currentStats`를 화면용 캐시로 여기고 그냥
통과시켰다. 테스트를 쓰다가 아니라는 걸 알았다 — 레벨업마다 성장률을 소수로 누적한
실제 상태이고, 그대로 믿으면 그 숫자만 고쳐 공격력 9999가 된다. 다행히 생성도
레벨업도 `종족 기본 + 성장률 × (레벨-1)` 을 만족하므로 완전히 유도할 수 있다.
서버는 종·성장률·레벨만 믿고 나머지를 다시 만들며, 그 셋은 각각 규칙 안에 있는지
따로 본다(성장률 상한·종 범위·기술 습득 목록·레벨 범위).

### 대전은 펫끼리 붙는다

처음엔 PvE와 똑같이 주인공 + 펫으로 짰다. **실측이 그게 틀렸다는 걸 보여줬다.**
펫 레벨이 10 차이 나는 두 세이브를 30판씩 붙였더니 승률이 13:17이었다 — 주인공
능력치가 양쪽 다 레벨에만 달려 있어 사실상 같고, 그 둘이 전투를 지배해 펫 차이가
묻힌 것이다. 게임의 축이 펫 육성인데 대전에서 육성이 무의미하면 대전할 이유가 없다.

주인공을 빼고 다시 재니 레벨 15 펫이 레벨 5 펫에게 태세와 무관하게 30:0으로
이겼다. 육성이 대전에 반영된다.

동급(같은 종·레벨·성장률)에서 100판씩 재봤을 때 한쪽이 56~61%로 앞서 보여 진영
편향을 의심했지만, 800판에 좌우를 바꿔 다시 재니 A진영 51.2%(0.71σ)였다. **앞의
수치는 표본 잡음이었다.** 편향은 없다.

대전에서 포획 태세는 쓸 수 없다. 남의 펫을 잡을 수 있으면 대전이 아니라 강탈이다.

### 에스크로 거래 — "거래 사기 복구 부재"의 해결

```
제안(offer) → 양쪽 잠금(lock) → 양쪽 확정(confirm) → 성립
```

**내용이 바뀌면 양쪽 잠금과 확정이 모두 풀린다.** 원작의 거래 사기는 대부분 상대가
확인한 뒤 조용히 품목을 바꾸는 수법이었고, 잠근 뒤에도 바꿀 수 있으면 잠금은 아무
것도 지키지 못한다. 확정은 둘 다 잠근 뒤에만 가능하다 — 잠그지 않은 채 확정할 수
있으면 한쪽이 확정한 뒤 다른 쪽이 바꾸는 길이 열린다.

원자적 교환만으로는 부족하다. 그건 사기를 **덜 일어나게** 할 뿐이고, 일어난 뒤에
되돌리려면 무엇이 오갔는지가 기록으로 남아야 한다. 그래서 모든 전이를 `trade_events`
에 한 줄씩 남기고, **각 줄이 그 시점의 내용을 통째로 담는다.** 지금 상태만 보관하면
바꿔치기가 있었다는 사실 자체가 사라진다. 취소·성립한 거래도 기록은 그대로 남는다.

### 실시간

`/ws?token=...` — 방 입장, 위치, 대화. 그게 전부다. **게임 상태는 이 통로로
오가지 않는다.** 위치와 대화는 조작돼도 남의 게임을 망가뜨리지 않지만 전투 결과와
거래는 그렇지 않고, 그래서 그쪽은 검증과 기록이 있는 HTTP 경로로만 간다. 실시간
통로를 "빠르니까"라는 이유로 상태 변경에 쓰기 시작하면, 검증을 붙일 자리가 없어진
뒤에 그걸 깨닫게 된다.

연결은 메모리에만 있다. 서버를 여러 대로 늘리면 Redis 같은 것이 필요하지만, 지금
넣어도 맞게 짰는지 확인할 방법이 없어서 넣지 않았다.

## 인증

Phase 8을 처음 낼 때는 인증이 없었다 — `player_id`를 본문·경로로 받아 그대로
믿었고, 남의 id를 적으면 남의 대전이 성립하고 남의 세이브를 읽을 수 있었다.
검증(`verify.ts`)이 아무리 촘촘해도 "무엇을 하는가"만 볼 뿐 "누가 하는가"는
안 봤기 때문이다. 그래서 계정과 토큰을 붙였다.

**계정** `server/auth.py`. 비밀번호는 계정마다 다른 소금을 붙여 `hashlib.scrypt`
(표준 라이브러리)로 늘려 저장한다. 없는 계정과 틀린 비밀번호는 같은 오류·같은
상태 코드를 돌려준다 — 다르면 로그인 시도만으로 어떤 이름이 존재하는지 알 수
있다. 없는 계정에도 해시를 한 번 돌려서, 응답 시간 차이로도 존재 여부가 새지
않게 한다.

**토큰** 서명 토큰(JWT)이 아니라 서버가 만든 난수를 DB에 저장한다. 서명 토큰은
서버가 상태를 안 가져도 되는 대신 **즉시 무효화를 잃는다** — 계정을 도난당했을
때 "다음 만료까지 기다리세요"는 답이 아니다. `/auth/logout`이 토큰 행을 그
자리에서 지운다.

**행위자는 언제나 토큰에서 나온다.** `/saves`·`/duels`·`/trades`·`/guilds`
전부 `player_id`를 본문·경로로 받지 않는다. 대전의 상대, 거래의 파트너처럼
"누구를 상대로"만 본문에 남고, "누가"는 `Depends(current_player)`가 토큰에서
꺼낸다. 세이브 목록·최신 세이브 조회도 경로의 `{player_id}`를 없애고
`/saves`·`/saves/latest`로 바꿨다 — 경로에 남아 있었다면 목록을 보는 것만으로
남이 언제 접속했는지 알 수 있었다.

**WebSocket도 예외가 아니다.** `/ws/{player_id}`였던 경로를 `/ws?token=...`로
바꿨다. 브라우저 WebSocket API는 연결 시 커스텀 헤더를 못 붙이므로 쿼리 문자열이
유일한 통로다. 토큰이 없거나 무효면 4401로 접속을 끊는다.

**테스트가 실제 버그를 두 개 잡았다.**

1. WebSocket 핸들러가 `Session(engine)`으로 전역 DB 커넥션을 직접 열고
   있었다 — 다른 라우트는 전부 `Depends(get_session)`을 쓰는데 이 핸들러만
   빠져 있었다. 프로덕션은 엔진이 하나뿐이라 겉으로 안 드러났지만, 테스트가
   인메모리 DB로 갈아끼우자 방금 만든 계정을 못 찾고 접속이 매번 끊겼다.
   FastAPI는 WebSocket 핸들러에서도 의존성 주입을 지원하므로 그대로 맞췄다.
2. 검증 테스트에 한글 문자열을 `Authorization` 헤더값으로 넣었더니 클라이언트
   라이브러리 단에서 인코딩 오류가 났다 — HTTP 헤더값은 라틴-1이라 애초에
   보낼 수 없는 값이었다. 서버 버그는 아니었지만, 그 사실 자체가 "가짜 토큰"
   테스트를 ASCII로 만들어야 하는 이유였다.

## 하지 않은 것

- **공성전.** 길드는 생성·가입·탈퇴·길드장 위임까지만이다. 공성은 시간대 예약,
  거점 상태, 다중 파티 전투, 보상 분배가 한 덩어리로 필요하고, 그 어느 것도 지금
  구조에서 확인할 방법이 없다.
- **비밀번호 재설정 · 2단계 인증 · 계정 잠금 · 이메일 확인.** 전부 실서비스에는
  필요하지만 각각이 메일 발송·복구 코드·잠금 해제 같은 별도의 흐름을 끌고 오고,
  그 흐름을 확인할 방법이 지금 없다.
- **거래 결과의 세이브 반영.** 서버는 무엇이 오갔는지를 확정하고 기록하지만,
  각자의 소지품에 실제로 옮기는 것은 클라이언트가 다음 동기화 때 한다. 서버가
  소지품을 소유하려면 세이브를 서버가 해석해야 하고, 그건 Phase 7에서 일부러
  피한 구조다.

## 기존 `client/` 디렉터리에 관해

이 저장소에는 헌장 이전에 만든 프로토타입이 `client/` 에 있다. 스택도 구조도
헌장과 다르다(상태관리 Context, 6스탯, Math.random 직접 사용, 밸런스 하드코딩,
테스트 없음). 헌장의 구조로 다시 세우는 중이며, 두 트리는 당분간 공존한다.

`client/public/pets/` 의 스프라이트 이미지와 `prompts.json` 은 새 트리에서도
그대로 쓸 수 있는 자산이므로 보존한다.
